import Yber from "../../../models/newUsers";
import dbConnect from "../../../utilis/dbConnection";
import   nc from 'next-connect'

dbConnect()

const handler = nc()

handler.get((req,res)=>{

    
    try {
        
    } catch (error) {
        
    }

})

export default handler